import { Meta } from '@storybook/react';

import SignUpLink from '@/components/projects/upload/SignUpLink';

export default {
  component: SignUpLink,
} as Meta<typeof SignUpLink>;

export const Default = {
  args: {},
  name: '기본',
};
