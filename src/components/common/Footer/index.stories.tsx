import { Meta } from '@storybook/react';

import Footer from '@/components/common/Footer';

export default {
  component: Footer,
  parameters: {},
  decorators: [],
} as Meta<typeof Footer>;

export const Basic = {
  args: {},
  name: '기본',
};
