import { Meta } from '@storybook/react';

import AboutMakers from '@/components/makers/AboutMakers';

export default {
  component: AboutMakers,
} as Meta<typeof AboutMakers>;

export const Basic = {
  name: '기본',
};
