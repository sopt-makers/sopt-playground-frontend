import { Meta } from '@storybook/react';

import UsingRules from '@/components/feed/upload/UsingRules';

export default {
  component: UsingRules,
} as Meta<typeof UsingRules>;

export const Default = {
  args: {},
  name: '기본',
};
